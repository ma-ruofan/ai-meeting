# AI 会议助理：两周求职 Demo 规划（Mac 开发＋API / Windows 部署＋llama.cpp）

> 更新：2026-09-07；岗位：AI 应用开发 / Agent 应用开发。
> 目标：第 7 天完成基础版，第 10 天冻结功能并开始投递，第 11～14 天评测、修复、录屏和整理简历。
> 排期假设：每天约 4～6 小时有效开发；Mac 使用大模型 API 开发，第一周可访问 Windows 验证本地部署；不是完成保证。
> 最终交付：整个应用在 Windows 运行，LLM 使用本地 llama.cpp，ASR 使用目标机验证过的本地模型。
> 当前状态：规划已更新，功能尚未实现，效果数字待实测。
> 原方案保存在同目录“简化版AI会议助理方案规划-Windows-原六周版备份.md”。本文件取代其首版范围和排期。

## 一、项目目标与范围

项目名：**本地 AI 会议助理——纪要生成与工具调用问答**。

完成两条流程：

1. 上传音频 → 转录 → 校正 → 摘要和行动项 → 点击证据播放原音 → Markdown 导出。
2. 用户提问 → 模型选择会议查询工具 → 程序执行并返回结果 → 模型带引用回答 → 展示调用记录。

纪要生成是固定工作流；问答是模型选择工具的有限循环，分别描述。主要展示模型集成、结构化输出、证据追溯、工具调用和评测能力。

### 必须交付的基础版

- WAV/MP3 上传，FFmpeg 转为 16kHz 单声道 WAV。
- faster-whisper 转录，保留片段 ID 和起止时间。
- 音频播放器、转录查看和文字编辑。
- 统一 LLM 接口生成摘要、决定、行动项；Mac 开发接云端 API，Windows 最终接 llama.cpp；不明确的负责人/截止时间为 null。
- Pydantic 校验结构，程序校验引用属于对应输入快照。
- 点击证据显示原文并从片段起点播放音频。
- SQLite 持久化、手动重跑失败阶段、Markdown 导出。

主样例 3～5 分钟；首版最多 10 分钟、普通话、单用户、一次一个音频任务。还需检查上下文预算，超过限制明确提示，不静默截断。不依赖自动说话人识别。

### Agent 版增加的内容

- 一个会议问答入口，三个只读查询工具。
- 模型选择工具和参数，程序校验、执行、回填结果。
- 最多 3 次模型响应、4 次工具执行；达到限制即停止。
- 展示实际工具名称、参数、结果摘要、耗时及答案引用。
- 无可靠证据时说明未找到明确依据。

Agent 功能只有实际跑通并验收后才写入简历。若进度不足，先投递已经完成的 AI 应用版。

### 两周内不做

自动说话人分离、录音、实时字幕、OCR、长会议分层总结、跨会议搜索、向量数据库、DOCX/PDF、批量处理、多用户权限、回收站恢复、完整诊断中心、分布式队列、微服务、多 Agent、外部邮件或任务创建。

FastAPI、React/Vue、LangGraph 不作为首版依赖。已有熟悉代码可复用，不为技术栈名称临时增加学习和集成工作。

## 二、技术架构

| 层级 | 选择 | 边界 |
|---|---|---|
| 页面 | Streamlit | 会议列表、转录与纪要、会议问答 |
| 业务 | Python 服务函数 | 页面不直接拼 SQL 或模型请求 |
| 音频 | FFmpeg＋faster-whisper | Mac 用 CPU 小模型或已转录样例；Windows 验证 CUDA |
| LLM | Mac 接云端 API；Windows 接 llama-server＋GGUF | 不强制重新部署或换成 14B |
| 请求与校验 | httpx＋Pydantic | 统一 LLM 接口，服务差异封装在 adapter |
| 存储 | SQLite＋本地音频目录 | 复杂结构用 JSON 文本保存 |
| Agent | 自写有限工具循环 | 原生调用优先，结构化动作协议可降级 |
| 评测 | Python 脚本＋人工标注 | 输出 JSON/CSV 和 Markdown 报告 |

```text
Streamlit
  ├─ 音频服务 → FFmpeg / faster-whisper 子进程
  ├─ 纪要服务 → LLM adapter → 云端 API（开发）/ llama-server（最终部署）
  ├─ 问答 Agent → 只读工具 → SQLite / 转录快照
  └─ 存储与 Markdown 导出
```

首版页面等待模型返回可以接受，显示真实阶段与耗时即可。任务由明确按钮触发，避免刷新重复执行；不建设持久任务队列和完整实时进度系统。

ASR 使用独立子进程，完成后退出。阶段完成后提交完整结果，失败可重跑该阶段，不承诺逐秒断点续传。模型请求设置超时，重复提交时禁用按钮。异常重启后只把确认进程已失效的运行记录标记为中断。

## 三、Mac 开发、API 接入与 Windows 本地部署

### 3.1 开发与部署分离

可以先在 Mac 使用大模型 API 完成界面、数据存储、纪要、引用、工具执行和评测。最终在 Windows 安装项目依赖与 FFmpeg，连接本机 llama-server。两端复用业务代码，不复制 Mac 的虚拟环境。

统一接口只保留两类操作：生成结构化纪要、完成一次带工具定义的对话。内部返回统一的内容、工具调用、结束原因和错误；各 provider 的请求格式、响应字段、用量统计在 adapter 中处理。页面和工具执行代码不依赖特定服务。

应用配置示意，名称为项目自定义项：

```text
# Mac 开发：替换为实际服务商配置
LLM_PROVIDER=cloud
LLM_BASE_URL=<服务商接口地址>
LLM_MODEL=<云端模型标识>
LLM_API_KEY=<通过未提交的环境变量提供>

# Windows 最终运行：base URL 在此约定包含 /v1
LLM_PROVIDER=llamacpp
LLM_BASE_URL=http://127.0.0.1:8080/v1
LLM_MODEL=<本地服务实际模型标识>
```

客户端约定统一：base URL 包含版本前缀，adapter 追加 /chat/completions，避免重复 /v1。若服务商协议不同则单独适配。API 密钥仅由 Python 端读取，进入 .gitignore 的 .env 或系统环境，不发送到浏览器或写入运行记录。本地认证按实际服务配置，不继承云端密钥。

切换主要通过配置与小范围 adapter 调整完成，但不能承诺只换地址即可：Schema 支持、工具调用、模型聊天模板、上下文限制和错误响应都要实测。云端和本地保留同一业务 Schema 和工具定义，避免首版依赖某一家托管 Agent、文件检索或其他专有能力。

Mac API 阶段按目标本地模型的预算限制输入，工具和输出保持简短；不依赖云端大上下文或强模型能力才能跑通的流程。开发阶段用虚构会议，明确此时文本会发送给 API 服务商；最终本地模式不自动回退到云端。页面标注当前 provider 和模型。

### 3.2 音频与系统兼容

- Mac 可先用已转录样例开发下游功能，样例明确标识为开发数据；最终仍须真实音频端到端验收。
- Mac 需要真实转录时，先试 faster-whisper CPU 小模型与短音频，不依赖 NVIDIA CUDA，也不为加速增加另一套 Mac 专属推理链路。Windows 再验证 CUDA 模式。参考 [faster-whisper 官方说明](https://github.com/SYSTRAN/faster-whisper)。
- 路径用 pathlib，数据根目录和 FFmpeg 路径通过配置提供，不硬编码 /Users/... 或 D: 路径。
- 子进程传参数列表，用 sys.executable 启动 Python worker；若使用 multiprocessing，入口增加 __main__ 保护并验证 Windows spawn 行为。
- 共用 pyproject.toml 和经两端验证的锁定依赖；平台差异用依赖标记或分组处理，不将 Mac 二进制包直接复制到 Windows。
- README 分别给出 Mac 开发命令与 Windows PowerShell 启动命令；LLM 暂不可用时仍能打开已有会议。

### 3.3 提前验证本地能力

第一天验证 API 的中文问答、目标 Schema、三类工具选择和非法参数处理。第 3～5 天在 Windows 用同一组文本验证本地模型，并跑一次短音频 CUDA 转录；第 7 天验证整个基础应用，第 9～10 天复测完整 Agent。

Windows 记录 build/commit、GGUF 文件哈希、量化、聊天模板、上下文和启动参数。不能因为云端 API 测试通过就将本地部署标为通过。

llama-server 提供 `/v1/chat/completions`、Schema 约束输出和健康检查。依据锁定 build 实测请求格式，不假定所有兼容接口行为完全一致。参见 [server 官方文档](https://github.com/ggml-org/llama.cpp/tree/master/tools/server)。

原生工具调用效果取决于模型、聊天模板和相关 Jinja 配置；接口接受 tools 不等于模型已正确调用。按当前 build 的帮助与 [function calling 官方文档](https://github.com/ggml-org/llama.cpp/blob/master/docs/function-calling.md) 验证。

- 本地先用已有模型；第一轮 Windows 测试发现工具选择不稳定时，最迟第 5 天完成一次备选验证或改用结构化动作协议，此后冻结。
- 模型标识通过已有服务确认，不使用 Ollama 标签或未经验证的名字。
- 应用配置包括服务地址、模型标识、超时、输入/输出预算、ASR 档位、数据目录。
- 为系统提示、工具结果和输出预留上下文空间。超预算明确提示，两周内不实现长文分层总结。
- ASR 与 LLM 推理串行，但 llama-server 空闲时仍可能占显存，必须实测驻留峰值。
- 放不下时优先降低 ASR 档位或减少 LLM 上下文；仍不行则手动停止项目专用 LLM 服务，转录后退出 ASR，再重启 LLM。
- 手动切换写入 README，不结束其他用途的模型服务，不另做复杂显存调度。
- 2080 Ti 的实际显存和稳定性用目标机验证，不承诺未经实测的速度和模型容量。

## 四、纪要生成与证据

输入每段包含 segment_id、start、end、text。输出示例：

```json
{
  "summary": "讨论了上传功能与下次评审安排",
  "decisions": [
    {"content": "首版只支持文件上传", "evidence_ids": ["S001"]}
  ],
  "action_items": [
    {"task": "整理接口文档", "owner": null, "deadline_text": null, "evidence_ids": ["S002"]}
  ]
}
```

规则：

1. 生成前保存有效转录快照和输入哈希，分析绑定快照。
2. 修改转录递增修订号，旧分析标记过期，历史引用仍显示旧快照。
3. 分析完成时再次比较修订号，运行中发生修改也要标记过期。
4. 决定和行动项必须有有效证据 ID，原文由程序取回，不依赖模型生成摘录。
5. Schema 或引用失败最多纠错一次，仍失败不保存为成功结果。
6. 引用合法不等于内容正确，建议是否被误当决定、负责人是否明确由评测和人工复核检查。
7. “周五”等时间先保存原文，不实现日期推算。
8. 只保存生成时的输入快照，不做逐键编辑历史。

## 五、最小会议问答 Agent

### 工具定义

| 工具 | 参数 | 返回 |
|---|---|---|
| search_transcript | keywords: list[str] | 命中片段和有限相邻片段、时间、ID |
| get_segments | segment_ids: list[str] | 指定片段和有限上下文 |
| list_action_items | owner: str 或 null | 当前纪要的行动项及证据 |

应用绑定当前会议和快照，模型不能跨会议查询。纪要与当前转录不一致时，行动项工具返回过期提示；模型改查转录或提示重新生成。

检索先用多关键词子串匹配、按命中数排序，每次最多 8 个片段并限制总字符数。README 如实写“关键词检索”；没有命中不证明会议一定没提到，不宣称实现向量 RAG。

### 执行循环

```text
用户问题＋工具定义
 → 模型选择工具或结束
 → 白名单与参数校验
 → 执行只读工具并返回结果
 → 模型继续查询或给出带引用答案
 → 校验引用，显示回答和执行记录
```

- 工具失败返回明确错误；不伪造执行成功。
- 避免重复调用同样工具和参数，模型响应与工具执行均有预算。
- 达到预算停止，返回已有证据及未完成原因，不强迫模型补全答案。
- 最终结果包含 answer 和 evidence_ids。涉及会议事实但缺乏有效证据时提示依据不足。
- 工具结果中的文字视为数据，不允许其改变工具白名单或执行任意指令。
- 页面显示实际工具调用，不需要展示模型内部思考。

原生 tool_calls 不稳定时，用 Schema 约束 `kind=tool/final`、工具名、参数等动作结构，由模型决策、程序执行并回填。简历可写“基于结构化动作协议实现工具调用循环”，不能声称未实际支持的原生 function calling。

演示问题：首版决定了哪些功能、哪些待办没有负责人、某个预算是否已经确认。必须允许换一个未硬编码的问题触发真实工具选择。

## 六、最少数据设计

| 表 | 内容 |
|---|---|
| meetings | 标题、音频相对路径、时长、阶段、修订号 |
| segments | 会议 ID、片段 ID、时间、原文、编辑文字 |
| analysis_runs | 输入快照 JSON、哈希、输出 JSON、模型/提示版本、状态 |
| agent_runs | 问题、输入版本、调用记录 JSON、回答、耗时、错误 |
| stage_runs | 阶段、状态、开始/结束时间、错误 |

片段 ID 以转录版本为范围。重新转录会替换当前片段，需明确提示，旧分析快照保留。文件先写临时文件再移动，数据库采用短事务。日志不记录密钥，样例采用虚构会议。

目录只需 app.py、services/、adapters/、storage/、prompts/、eval/、scripts/ 和 README.md，不为层级本身增加抽象。

## 七、14 天排期

| 天 | 工作 | 完成标准 |
|---|---|---|
| D1 | Mac API、统一 LLM adapter、Schema/工具调用验证 | 固定文本生成结构化结果，验证短音频 CPU 转录 |
| D2 | API 版命令行闭环 | 短音频→转录→纪要；3 分钟样例可复用已保存的真实转录 |
| D3 | 上传、播放器、转录、SQLite | 重启可查看；开始 Windows 依赖安装 |
| D4 | 纪要页、结构与引用校验 | 点击证据查看原文并播放；Windows 本地模型小样例验证 |
| D5 | 编辑、输入快照、Markdown | 旧引用可查看；Windows 短音频 CUDA 转录及模型协议冻结 |
| D6 | 超时、错误提示、阶段重跑 | LLM 不可用不破坏已有结果 |
| D7 | Windows 基础版联调与录屏 | 全应用在 Windows 跑通，整理简历草稿 |
| D8 | 三个查询工具、Agent 循环 | 工具选择和执行真实可见 |
| D9 | 问答界面、证据和边界、Windows 本地 Agent | 本地模型上业务问题及无依据问题均可演示 |
| D10 | Windows 小规模评测与阻断修复 | 功能冻结，开始投递 |
| D11～12 | 完整评测、README、架构图 | 简历数字能追溯到报告 |
| D13～14 | 2～3 分钟录屏、简历定稿、投递 | 仓库和视频可访问，能独立讲解 |

止损规则：

- 环境问题不额外占用超过 1 天，及时降 ASR 档位或复用已验证模型。
- D5 未闭环：移除转录编辑和历史版本页面，保留输入快照及证据显示，简历删掉未实现能力。
- D7 基础版未完成：优先交付基础版和评测，Agent 根据余量决定。
- D10 后只修演示和正确性问题，不新增模型、框架或功能。
- 第一周无法访问 Windows 时，继续完成 API 版，但本地部署与性能保持“待验证”；投递时只描述已实现范围。
- 若每天只有 1～2 小时，直接采用基础版，不承诺两周同时完成 Agent。

## 八、轻量评测

准备 3 段自录音频，每段 3～5 分钟，覆盖清晰会议、数字/专有名词、未定事项；另外准备 10 段短文本评测纪要、20 个问题评测 Agent。调提示样例与验收样例分开，报告实际样本量。

| 指标 | 记录方式 |
|---|---|
| ASR | 音频时长、加载/推理耗时、RTF、人工标注片段 CER |
| Schema | 首次通过数/总数，纠错后通过数/总数 |
| 行动项 | 正确提取、误提取、漏项，计算 precision/recall |
| 证据 | 引用合法率和人工判断的内容支持率分别记录 |
| Agent | 问题成功数/总数、参数合法率、调用次数、耗时 |
| 无依据 | 是否错误补全负责人、日期、决定，能否说明证据不足 |

同一模型做一次小对照：普通提示输出 vs Schema＋引用校验，比较格式与非法引用。不要把非法引用拦截效果解释为语义正确率改善，也不把小样本数据描述为普遍准确率。

云端 API 与 Windows 本地模型的报告分开，记录 provider、模型和运行平台。最终本地指标必须在 Windows 重跑；至少验证结构化纪要、工具选择/结果回填、无依据回答、超预算和服务失败。模型缓存就绪后做一次 Windows 断网端到端验收，确认没有隐式云端回退。

自动检查只做关键项：非法引用拒绝、输入快照不被覆盖、非法工具名/参数拒绝、预算耗尽停止。其余用可重复的人工验收。

最终本地版发布条件：全应用在 Windows 上真实音频到纪要跑通；引用对应正确快照和音频位置；重启能查看；服务失败不破坏已有数据；若写 Agent，必须有本地模型的真实工具选择和执行证据；至少一份 Windows 完整录屏与评测报告。未通过之前可发布已验证的 API 开发版，但不宣称已完成全本地部署。

## 九、简历与交付物

下面是对应功能完成后的模板，当前不能直接作为已实现成果：

- 基于 Python、faster-whisper 和 llama.cpp 构建本地会议助理，完成音频转录、结构化纪要和行动项提取、证据定位与 Markdown 导出。
- 通过 Pydantic、转录输入快照及引用校验实现生成结果追溯，支持人工校正后识别纪要过期。（只保留已实现部分。）
- 实现会议问答 Agent，由模型选择转录检索、片段查询和行动项查询工具，加入参数校验、调用预算及执行记录，输出带原文证据的回答。（仅 Agent 验收后使用。）
- 在 X 段音频、Y 条文本、Z 个问题上评测 RTF、提取质量、引用支持率与问答成功率。（只填实测数字和清楚口径。）

交付：源码、依赖锁文件、启动 README、截图、2～3 分钟视频、评测报告、1 页架构和限制说明。仓库不含模型权重、密钥、真实敏感会议。

面试需独立解释：为什么用 llama.cpp、Schema 解决什么、引用合法与内容正确的区别、模型如何选工具、程序如何执行、循环如何停止、错误如何处理、关键词检索的局限、评测数据如何得到。

不写未发生的生产用户量、高并发、模型训练、多 Agent、向量 RAG 或百分比提升。以已实现、能复现、能解释的成果投递。
