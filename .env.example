# Configuration reference only: the application does not automatically load .env.
# Set these variables explicitly in your shell before starting the backend.
# See README.md for details. Never commit real API keys.
XIAOK_LLM_MODE=mock
XIAOK_LLM_URL=
XIAOK_LLM_MODEL=
XIAOK_LLM_KEY=
XIAOK_ASR_MODEL=base
# Optional paths (defaults are relative to the project directory):
# XIAOK_MODEL_DIR=models
# XIAOK_DATA_DIR=data/v2
