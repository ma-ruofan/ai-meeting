# AI 会议助理：个人开发与简历展示版

更新日期：2026-09-07

## 1. 项目目标与范围依据

用户明确目标：个人开发，完成后与几位朋友使用多台设备模拟测试，作为简历项目经历；不按商业产品的复杂度实施。

用户追加确认：声纹识别和语音“小K”均为必做功能，不能作为选做项或仅以人工标注、按钮问答替代。简化的是部署与商业功能，保留这两项核心能力。

用户确认的补充约束：最终也要在 Windows 上运行；求职方向偏 AI 应用开发和 Agent 应用开发。文本大模型暂不选定，本地模型或云 API 由用户后续决定，不作为工程启动前置条件。用户技术基础、Windows 设备配置及每周投入尚未提供，不据此承诺完成日期。

本文件据此给出精简实施建议。原《方案规划.md》保留为功能参考，其中的 14 人团队、36 周排期、企业运营、商业化和大规模部署要求不作为本次开发验收范围。下面的技术路线及周期为初步建议，应结合开发者基础、求职方向与每周投入调整。

## 2. 首版演示故事

参会者先在主电脑上分别登记声纹。主持人创建会议，朋友通过同一局域网中的手机或电脑加入，并选择本次允许使用的声纹档案。一台主电脑采集会议室麦克风，系统转录并匹配说话人，其他设备同步查看。会中说出“小K小K”后提出问题，助理根据已有会议内容回答并通过主电脑扬声器播报。结束后生成摘要、决定和待办，点击引用可定位原始发言，最终导出会议纪要。

先支持线下同室模拟会议。多设备表示多个客户端连接同一个服务，不表示实现了多人音视频通话。首版不采集第三方会议软件系统音频。

## 3. 必做功能

1. 创建会议、邀请加入、填写昵称，区分主持人与参会者。
2. 主电脑麦克风录音，以及上传已有音频；录音有明确开始、停止和错误状态。
3. 语音转文字：先完成文件转录，再接入分段处理、持续追加字幕的准实时模式；实际延迟以测量为准。
4. 多设备同步：WebSocket 推送稳定记录；刷新和短暂断线后从服务端恢复已有记录。
5. 文本修正：主持人修改转录，修改结果同步到其他客户端。
6. AI 会议摘要、决定、待办及选段问答。回答附发言 ID，缺少证据时明确说明；不把推测的负责人或日期当成会议事实。
7. 历史会议、Markdown 导出、删除会议及关联音频。
8. 声纹登记、会中识别、未知说话人保留、人工纠错、声纹档案删除。
9. 语音“小K”：固定唤醒词、收取问题、ASR、会议上下文问答、TTS 播放、停止按钮和失败后文字显示。

### 3.1 声纹识别的个人版边界

- 演示目标先设为 3～5 位参会者，同室、单支主麦克风、以轮流说话为主。
- 每人登记多段干净语音，提取预训练模型的说话人嵌入，与本人选择的显示名关联。本项目不从零训练声纹模型。
- 按语音窗口和说话人变化处理音频，再与本场已授权的候选声纹比较；不能假设每个 ASR 句子只有一个说话人。
- 同时检查语音质量、相似度阈值和第一/第二候选分差；不足以判断时显示“未知/待确认”，不强制指派最相近的人。
- 检测到重叠或不能可靠分配的片段保留“多人/待确认”，不承诺分离多人同时讲话的全部内容。
- 声纹相似度不显示为身份正确概率。人工纠错与自动匹配分别记录，不自动用纠错音频更新登记模板。
- 先验证录音文件中的识别，再接入准实时链路。模型分数、阈值和支持距离以留出的测试录音验证；小样本结果只描述朋友测试范围。
- 声纹保存在主电脑，绑定昵称用于会议标注；不作为登录认证。提供本人撤回本场使用和删除档案入口，撤回后停止后续匹配，历史标注按会议记录管理。

### 3.2 语音“小K”的个人版边界

- 默认固定唤醒短语“小K小K”，先验证中文与字母发音的实际识别效果，不扩展任意自定义唤醒词。
- 状态流程：待唤醒 → 收听问题 → 生成回答 → 播报 → 返回待唤醒；有明确界面状态、收音结束规则和超时退出。
- 唤醒后保留短预录缓冲，避免丢掉紧接唤醒词的问题开头；以静音检测结束问题，并设置最长收音时限。
- 问答以请求时已有的稳定会议记录为依据，附发言引用；证据不足时明确说明。所有参会者可见的回答只使用共享记录。
- TTS 通过主电脑播放固定合成音色，语音AI问答单独显示，可进入纪要但必须标明AI来源，不作为人类会议事实。
- 首版采用半双工：播放 TTS 时暂停会议转录、声纹识别和唤醒检测，播放完成并经过尾音保护后恢复，界面明确显示“播报中暂停记录”。播报时的人声不保证被记录；原始测试录音若保留该区间，后处理也必须排除或明确标记，不能重新送入事实链路。
- 播报期间提供可立即停止播放并取消当前回答的按钮；语音打断与全双工回声消除后置。语音唤醒和口头提问仍是必做，按钮仅作控制与备用入口。
- ASR/LLM/TTS 失败分别显示原因，允许重试；TTS 失败保留文字答案，避免错误状态卡住后续唤醒。

### 3.3 已确认的小K回答入纪要规则

- 直接保存原始问题、生成答案、时间、引用和“AI回答”标记，各设备同步查看；不通过重新转录扬声器声音保存答案。
- 会后纪要将未采纳内容单独列为“AI建议”，不把它自动记为参会者事实、会议决定或待办。
- 用户明确采纳后才转为正式决定或待办，保留对应回答和确认来源。首版采用主持人点击确认；口头采纳可由模型提出待确认候选，不能仅凭模型猜测直接发布决定。
- 检索与总结区分人类发言、AI回答及已确认成果，防止模型把自己先前的建议循环引用成会议事实。

## 4. 选做加分项

核心流程稳定后，最多先选一项：

- 复杂重叠发言下的说话人分离、跨设备声纹稳健性。
- “小K”自定义唤醒词、语音打断与全双工回声消除。
- 带引用的长会议检索问答，并对比直接上下文与检索方案的效果。
- 改善录音分段缓存、失败重试与重连恢复，并提供故障演示。

这些能力不阻塞首版。目标是实现并解释少量有深度的工程能力。

## 5. 暂不实现

- 企业组织管理、短信登录、收费订阅、运营后台。
- 跨组织中央声纹库、大规模身份检索、声纹登录认证；小规模登记匹配与阈值验证属于必做。
- 自建视频会议、TRTC、腾讯会议/飞书/钉钉独立机器人。
- Windows/macOS 原生桌面客户端、系统音频捕获、签名自动更新。
- 跨区域部署、多租户商业平台、微服务集群和大规模并发。
- 自动参加会议、自动发送消息、自动执行会议待办。

本阶段仅做个人与朋友的受控测试，商业公开上线属于新的项目范围。

## 6. 精简技术方案（建议）

| 部分 | 初步选择 | 作用 |
|---|---|---|
| 前端 | React + TypeScript | 浏览器界面、录音控制、记录与问答展示 |
| 后端 | Python + FastAPI | API、会议状态、WebSocket 与模型编排 |
| 存储 | SQLite + 本地音频目录 | 会议、成员、稳定发言、任务与文件 |
| 语音识别 | 一个 ASR 适配器 | 先基于现有电脑实测本地模型；不满足演示速度时选云接口 |
| 声纹 | 预训练说话人嵌入模型，先验证 WeSpeaker 中文路线 | 登记、分段嵌入、相似度匹配与拒识 |
| 唤醒 | sherpa-onnx KWS 候选路线 | 在主机检测固定唤醒词，模型及发音配置先实测 |
| 语音合成 | 一个中文 TTS 适配器 | 固定音色播报，可取消；先实测本地方案，不满足时选云接口 |
| 大模型 | 可替换模型适配器，供应商待定 | 本地模型/云 API 后续接入；开发时提供明确标识的模拟模式 |
| 部署 | 主电脑单机运行 | 朋友的设备作为局域网客户端 |

FastAPI 官方提供 WebSocket、多客户端及断开连接处理示例：
https://fastapi.tiangolo.com/advanced/websockets/

模型候选依据：WeSpeaker 官方提供中文模型、嵌入提取、相似度与说话人分离接口；sherpa-onnx 官方提供关键词检测文档。它们不保证本项目实际设备上的速度、准确率或“小K小K”的开箱效果，需要小样验证并核对所用权重许可。
https://github.com/wenet-e2e/wespeaker
https://k2-fsa.github.io/sherpa/onnx/kws/index.html

首版采用单个业务服务和独立音频工作进程，音频采集向 ASR、声纹和唤醒检测分发，避免模型推理阻塞 API。限制任务队列，优先保障收音和唤醒，长音频处理不能无限排队拖慢问答。稳定记录与任务状态持久化；内存连接列表只用于推送。暂不引入 Redis、独立向量数据库或 GPU 集群。

前端按服务端分配的发言 ID 去重，通过顺序号获取遗漏记录。服务重启后客户端重新连接并加载持久化数据，不承诺无人值守持续录音恢复。

短会议优先使用有长度限制的原文上下文及发言 ID。超过模型输入预算时明确分段总结或提示限制，不能悄悄截断后宣称覆盖全部会议。

### Windows 与 macOS 运行要求

- 两个平台都作为主机运行目标：主机启动 Python 服务和前端，使用本机浏览器采集麦克风；其他设备通过局域网浏览器加入。跨平台运行不要求原生桌面安装包。
- 提供 Windows PowerShell 与 macOS shell 启动入口，共用核心 Python/TypeScript 代码。路径、进程启动、音频解码和模型资源定位按跨平台方式实现。
- 浏览器采集、服务端 ASR/声纹/KWS 和 TTS 都需在两个系统验证，不能把 macOS 的系统语音命令作为唯一 TTS 实现，也不能把 Apple 专用加速作为必需条件。
- 默认先验证 CPU 可运行路线，根据实际硬件启用可选加速。模型文件单独下载并记录版本，依赖在两个平台分别验证后锁定。
- 自动化测试区分业务逻辑与音频硬件；Windows 构建/单元测试不能替代 Windows 真机麦克风、扬声器和长会测试。

### AI / Agent 应用开发重点

- 摘要与待办采用可复现的固定工作流，输入输出使用结构化 Schema，校验发言引用和字段完整性。
- 小K使用一个受限会议 Agent：按问题决定是否调用会议检索、读取指定发言、查询已确认待办等工具，再组织带引用的答案。首版不引入多 Agent 协作。
- 工具示例为 `search_meeting`、`get_utterances`、`list_confirmed_actions`。工具在服务端绑定当前会议和成员权限，模型传入的 ID 不能扩大读取范围。
- 实现真实的“模型请求工具 → 服务端校验执行 → 工具结果返回模型 → 最终回答”循环，并限制步数、时间和上下文预算；只把多个 Prompt 串起来时应称工作流。
- 模型适配层区分结构化输出、原生工具调用、流式输出等能力。缺少原生工具调用时，可使用严格校验的 JSON 工具协议，达不到协议要求则明确报错或退回固定问答流程。
- 开发模拟模式用固定样例覆盖工具选择、参数错误、超时和重试，在页面及日志明确标记。它不计入真实模型质量或 Agent 自主决策验收。
- 记录每次任务的输入引用、工具名称、参数、结果状态、耗时和模型配置，展示可解释的执行轨迹；不记录或要求模型隐含思维链。
- 建立小规模评测集，覆盖有答案、无证据、错误引用、工具失败、越权请求和会议原文中的恶意指令。比较固定问答与工具式问答的正确性、引用质量、延迟和调用成本。
- 首版工具只读取会议数据。AI建议的采纳由明确的用户操作完成，不让模型自行发送消息或执行待办。

### 局域网录音安排

服务电脑的录音页面通过 localhost 打开；朋友通过局域网地址加入，首版只查看和操作文字，不调用麦克风。网页麦克风要求安全上下文；普通局域网 HTTP 地址不能直接作为跨设备录音方案。如果以后允许其他设备录音，需要配置设备信任的 HTTPS 并重新测试权限。

浏览器录音条件参考：
https://developer.mozilla.org/en-US/docs/Web/API/MediaDevices/getUserMedia

## 7. 用户开始前需要准备

- 一台开发电脑，记录系统、内存和可用空间；先实测，不预先采购 GPU 或长期云服务器。
- 两三台可借用的手机或电脑、同一 Wi-Fi、一个可用麦克风。
- 一份自己录制或获准使用的短音频，以及几个愿意配合录音测试的朋友。
- 声纹小样：先请 3～5 人分别录制三段 10～20 秒的清晰语音作为登记起点；另外在不同时间录制独立测试语音，不能用登记音频证明识别效果。具体时长按选定模型实测调整。
- 至少一位不登记声纹的测试者或经授权的未登记语音，用于检查系统是否会误认成熟人。
- 可用扬声器，以及“小K小K＋问题”、普通谈话、相似发音等唤醒测试素材；保留真实扬声器播报场景检验自唤醒。
- 文本大模型后续再选：选择云 API 时准备服务地址、模型名和密钥；选择本地模型时准备模型服务与实际运行配置。当前无需提供密钥或下载大语言模型。
- 每月模型调用费用上限。ASR/TTS 是否付费调用待实测决定，开通前核验价格和额度。
- 希望投递的岗位、已有技术基础、每周能投入的时间，用来调整技术深度与节奏。

已确定求职方向为 AI/Agent 应用开发。当前主机检查结果为 Apple M1、8 GiB 内存、macOS 15.1，工作磁盘可用约 103 GiB。Node/npm/pnpm 可从当前命令环境找到，uv、ffmpeg、ffprobe 未在当前 PATH 中找到；尚未完成依赖版本和现有虚拟环境验证。先控制模型常驻数量并测量资源占用，不假设所有语音模型与本地大语言模型可以同时流畅运行。

Windows 真机在跨平台联调时需要提供，开发起步不要求用户现在购置新设备。

工程环境安装、接口设计、任务拆解、代码、测试脚本和使用文档可在开发过程中完成，不要求用户先自行准备企业级基础设施。

测试前让朋友知道何时录音、声纹用途、音频和声纹保存位置、是否调用外部模型及如何撤回或删除。声纹登记与普通入会分别确认；声纹向量及原始登记录音不进入代码仓库。API 密钥只存服务端，不打包进前端；邀请凭证具有时效，接口和 WebSocket 都校验成员身份，客户端自行传入昵称不能获得主持人权限。

## 8. 开发顺序与完成条件

此前仅含基础转录与问答的 4～6 周估计不再沿用。声纹匹配、唤醒和播报会增加音频联调工作，先完成阶段 0，再结合每周投入估计日历周期。

| 阶段 | 内容 | 完成条件 |
|---|---|---|
| 0：核心音频验证 | 声纹小样、固定唤醒词、TTS 播报隔离、主机负载 | 用留出的真实语音证明匹配/拒识和唤醒问答可运行；记录错误和延迟 |
| 1：工程与文字闭环 | 页面、会议、SQLite、音频上传、转录 | 一段真实音频能够生成并保存文字 |
| 2：AI 与成果 | 模型适配器、结构化摘要/待办、受限会议Agent、引用、导出 | 模拟模式通过流程测试；选定真实模型后补齐回答与工具调用评测 |
| 3：多设备与录音 | 麦克风、分段转录、WebSocket、重连 | 主机录音，至少两个其他客户端同步显示 |
| 4：声纹集成 | 登记、授权、会中匹配、人工纠错、撤回和删除 | 已登记朋友可自动标注，未知/不确定片段不强制实名，各端同步结果 |
| 5：语音小K集成 | 唤醒、问题收音、带引用问答、TTS、停止和状态控制 | 真实口头唤醒后完成问答播报，恢复后能继续记录，无自触发循环 |
| 6：测试与展示 | Windows/macOS 真机测试、朋友实测、Agent评测、异常修复、文档、演示 | 两系统核心流程及声纹、小K均通过真实设备/模型测试，有启动文档和演示视频 |

每阶段先展示一个真实可运行结果，再进入下一阶段。模拟转录可用于早期界面开发，但最终验收必须使用真实音频和真实模型输出。

## 9. 朋友测试方案

准备三场 10～20 分钟的模拟会议：一次安静讨论、一次包含专业词和插话、一次包含断线重连。每场有明确议题、决定、任务负责人和时间，方便核对。

补充专项素材：已登记和未登记者、不同日期/距离的发言、短句和交替发言、重复唤醒、无问题超时、连续提问及 TTS 扬声器回录。

必须验证：

- 一台主机录音，至少两个其他设备加入并看到一致的稳定文字。
- 一个客户端断网再连接，恢复记录无重复；无权进入会议的客户端无法读取正文。
- 刷新页面后记录仍在；重复请求不会重复创建同一份处理结果。
- ASR/LLM 请求失败有可理解的提示和重试入口，已有记录仍保留。
- 修改文字后，旧 AI 成果标记可能过时或支持重新生成。
- 摘要和待办能够人工核对，引用能跳到对应发言。
- 删除会议后列表、正文、音频与导出缓存不再提供访问。
- 登记音频之外的新发言能够匹配本人；错误匹配、拒识、未知者误接纳分别记录，人工纠错不计作模型识别成功。
- 撤回本场声纹授权后停止后续匹配；删除档案后向量和登记录音不再参与识别，普通成员不能读取他人的声纹数据。
- 真实说出“小K小K”并口头提问，可得到文字与语音回答；普通谈话和相似词的误唤醒有记录。
- 播报不会被重新识别人类发言或重复唤醒；停止按钮有效，播报后录音恢复。播报期间测试者说话，界面明确说明暂停记录限制。
- 连续两次语音问题、收音超时和任一模型服务失败后，状态都能回到可继续使用。
- 未采纳的AI回答不出现在正式决定中；主持人确认采纳后保留来源，导出与页面的分类一致。
- Agent 工具真实执行、返回结果后形成引用；未知工具、参数错误、权限失败、步数超限均有确定处理方式。
- Windows 与 macOS 分别验证启动、音频上传、麦克风采集、声纹、唤醒、TTS、停止与多设备同步；报告按平台分别记录。

记录设备配置、音频时长、转录耗时、从片段结束到稳定文字显示的延迟、客户端数量、错误类型、摘要和引用的人工评审结果。专项增加声纹匹配/拒识统计、未知者误接纳、唤醒命中/漏检、每测试小时误唤醒次数、问题结束到首段语音播放的延迟。样本不足时报告范围及样本数，不编造高精度百分比或大规模并发结论。

## 10. 简历与面试交付物

- 可运行代码仓库与清晰 README：安装、配置、启动、演示步骤。
- 一张架构图及技术取舍说明，覆盖声纹阈值/拒识、Agent工具循环、模型适配、语音问答状态控制、播报隔离、断线补齐和引用校验。
- 一个 3～5 分钟演示视频，覆盖声纹登记与匹配、真实唤醒问答、多设备同步和一次故障恢复。
- 一份朋友实测报告：环境、流程、实测指标、已知限制。
- 完成后根据真实实现撰写简历，区分个人工作、模型调用和开源组件；用实际设备数与实测结果描述规模。

简历描述模板（完成后填入实测值）：

“独立开发 AI 会议助理，基于 Python/FastAPI 与 React 实现录音转写、登记声纹匹配、多端记录同步和带引用的会议总结；集成关键词唤醒、语音问答与 TTS 播报，设计未知说话人拒识、播报隔离及断线补齐机制，在 [实际设备数] 台设备、[实际会议场数] 场模拟会议中完成验证。”

## 11. 当前目录处理

当前 Git 工作区存在旧 Python 原型的已删除文件，新完整规划尚未纳入提交。正式代码开发前检查并保存可回溯基线，保留用户删除意图；不能自动恢复旧代码覆盖工作区。现存 .env 和 .venv 的适配情况待检查，密钥内容不输出到日志或文档。

## 12. 2026-09-08 首轮代码落地记录

已按本规划建立 `backend/` 和 `frontend/`，实现会议管理、多设备快照同步、录音转写、声纹登记匹配、固定唤醒词、语音问题状态流、受限Agent工具协议、AI回答独立入记录、主持人采纳及导出。文本大模型仍保持模拟模式，等待用户之后选择本地或API。

第一轮的具体边界：声纹档案先以单场会议管理；首版使用短窗口匹配，不承诺多人重叠分离；语音问题处理及播报期间均暂停接纳新的会议音频；浏览器TTS只选本地中文音色；主录音设备离线续录尚未实现。两端启动脚本和CI矩阵已经编写，Windows真机和真人样本效果仍待验证。

运行方式见 `README.md`；真实执行过的验证与限制见 `docs/verification-v2.md`。这些实现状态与原规划中的目标分开记录，不能把未实测项目标记为已验收。

### 现场无设备参会补充

支持主持人直接添加现场参会者，无需每人携带手机或建立浏览器身份。在本人同意后，由主持人录制或上传该成员的声音以登记声纹，支持代为撤回、删除；会议统一收音与本地保存方式保持一致。现场成员与设备成员合计每场最多12人。

### 私聊与语音问答分离（2026-09-08用户确认，取代旧版文字共享问答）

右侧文字聊天为个人私聊，按成员隔离，不写入纪要、不导出、不允许采纳到共享决定。私聊可参考人类记录、共享语音问答、纪要草稿及本人最近私聊历史。会议结束后自动删除所有私聊并关闭入口。语音唤醒或语音提问走独立链路，问题与回答自动保存为共享语音问答并进入纪要附录；语音Agent不得读取任何私聊。AI语音建议仍需主持人确认才能成为决定或待办。
